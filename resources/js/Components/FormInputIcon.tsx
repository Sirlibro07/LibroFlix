import React from "react";
import Icon from "./Icon";

const FormInputIcon = ({ icon }) => {
    return <Icon icon={icon} className="absolute left-4 text-xl opacity-50" />;
};

export default FormInputIcon;
